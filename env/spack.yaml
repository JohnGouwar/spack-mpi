spack:
  config:
    extensions:
      - /path/to/spack-mpi
  specs:
  - clustcc
  - clingo@spack
  - flux-sched
  view: true
  concretizer:
    unify: true
  repos:
    extensions:
      git: 'https://github.com/JohnGouwar/spack-extension-packages.git'
